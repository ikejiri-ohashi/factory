Hello world form index

<%= link_to "加工依頼の登録", new_job_path, class:""%>

<% @jobs.each do |job|%>
  <%= job.name %>
  <%= job.place %>
  <%= job.deadline %>
  <%= job.category.name %>
  <%= job.memo %>
  <%= job.contact %>
<% end %>